  <h1 class="mt-4">Dashboard</h1>
  <p class="lead mb-3">Welcome <%= user.name %></p>

  <p class="lead mt-3">Here are you account information:</p>

  <ul>
    <li class="lead">Email Address: <%= user.email %></li>
    <li class="lead">Number of Reminders: <%= user.reminders.length %></li>
  </ul>
  <% if(user.role === "admin"){ %>
  <a href="/admin" class="btn btn-primary">Admin Options</a>
  <% } %>
  <a href="/auth/logout" class="btn btn-secondary">Logout</a>